spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gb-db
    username: mus
    password: mus
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
jwt:
  secret: secret
  lifetime: 30m